namespace demo;

table TextInput { text:string; }
table Event { code:int32; }
table StringValue { value:string; }
union Payload { TextInput, Event, StringValue }

table AgentRequest {
  session_id:string;
  payload:Payload; // flatc will also generate payload_type implicitly
}

root_type AgentRequest;